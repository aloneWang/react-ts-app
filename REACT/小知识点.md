### React.lazy 
动态引入 React 组件

### 使用方法
```
const otherComponent = React.lazy(()=> import('./otherComponent'))
```
React.lazy 接受一个函数， 函数必须返回一个 `promise` , 这个 promise 会 resolve 一个 export default 的React 组件
结合 suspense 组件 可以做一些 加载过程中的 优化

### 代码
```
import React, {suspense} from 'react'
const OtherComponent = React.lazy(()=> import('./otherComponent'))

function MyComponent() {
    return (
        <Suspense fallback={<div>loading....</div>}>
            <OtherComponent />
        </Suspense>
    )
}
```
### suspense 组件
fallback 属性接受在加载过程中 展示 的react 元素

### 源码解析


### html role 属性
增强语义性
比如：`<div role="button"></div>` 表示这是个button,
增强交互性，语义化， 访问


### ts 类型解构 小巧用
```
    interface Tab {
        key: string;
        tab: any;
        disabled: boolean;
    }
    interface TabNodeProps {
        a: string;
        b: string;
        tab: Tab
    }
    function TabNode({
        a,
        b,
        tab: {key, tab, disabled}
    }: TabNodeProps) {

    }
```
这里面 tab 属性解构